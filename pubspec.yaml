name: kekemelik_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  permission_handler: ^11.0.1 # Mikrofon izni
  record: ^7.0.0 # Ses kaydı
  path_provider: ^2.1.1 # Dosya yolları
  just_audio: ^0.9.36 # Ses çalma
  http: ^1.1.0 # HTTP istekleri (Backend iletişimi)


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true